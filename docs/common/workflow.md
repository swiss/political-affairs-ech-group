# Documentation Workflow

Manual steps:

- Description of the data schema in YAML using LinkML in the `input` folder of each subgroup in the file `schema.yaml`.
- Generate example data conforming to the schema in the files `data_xyz.yaml` in the `input` folder.
- Write the accompanying documentation in Markdown with files in the form of `1_head.md`, `2_intro.md` (`number_subject.md`) in the `input` folder.

Automated steps (every commit that changes files in the `input` folder triggers a GitHub Action that automatically):

- Generates a schema in JSON and OWL (TTL) format from the `schema.yaml` file and stores it in the `output` folder as `schema.json` and `schema.ttl`.
- Converts the example data in `data_xyz.yaml` to JSON and RDF (TTL) format and stores it in the `output` folder as `data_xyz.json` and `data_xyz.ttl`.
- Generates the documentation using the `linkml-doc` tool. The generated documentation is stored as individual Markdown files in the `output/docs` folder.
- Creates a unified Markdown file from all the files in the `input` folder and stores it as `output/documentation_merged.md`.
- Converts the unified Markdown file from the `output` folder to DOCX using `pandoc` and a `template.docx` file in the `input` folder and stores it in the `output` folder.

## Building the DOCX Document

The final Word document is assembled from **hand-written prose** (the numbered `input/*.md` files) interleaved with **auto-generated LinkML docs** (`output/docs/*.md`). The pipeline is: `gen-doc` → `merge_documentation.py` → `pandoc`.

### Page Layout and Ordering

- **Ordering**: `merge_documentation.py` globs `input/*.md`, sorts them **by filename**, and concatenates them. The numeric prefixes (`01_head.md`, `02_introduction.md`, `03_person.md`, …) therefore define the chapter order in the document. Keep the numbering zero-padded and consistent.
- **Title page / metadata**: `01_head.md` carries the pandoc YAML frontmatter (`title`, `lang`, `toc`) and the eCH metadata table. It is the only file that does **not** start with a page break.
- **Page breaks**: every chapter file (from `02_*` onward) begins with the pandoc raw directive `\newpage` on the first line to force a **new page** in the DOCX, followed by the chapter heading and prose.
- **Styling**: `pandoc` applies the Word styles from `input/template.docx` via `--reference-doc`. Adjust fonts, headings and the cover layout there, not in the markdown.

### Include Autogenerated Documentation

To pull an autogenerated Class/Slot/Enum/Type doc from `output/docs` into a chapter, put an include directive on its own line:

`{{include:path_to_md_file}}` e.g. `{{include:tutorial/output/docs/AgendaItem.md}}`

- **Paths are repo-root-relative**, not relative to the including file — e.g. from `ech-0294_actors/input/04_organe.md` you still write `{{include:ech-0294_actors/output/docs/Group.md}}`.
- When merging, `merge_documentation.py` rewrites each included file so it links up correctly in one document: the heading `# Class: Group` becomes an anchor `# Class: Group{#Group}`, and cross-links `[Group](Group.md)` become internal links `[Group](#Group)`.

### Adding New Schema Content to the Document

1. Define the class/slot/enum in `input/schema.yaml`. On the next CI run `gen-doc` emits `output/docs/<Element>.md`.
2. In the appropriate `input/NN_topic.md`, add an `{{include:<subgroup>/output/docs/<Element>.md}}` line — together with an optional `\newpage`, a heading and explanatory prose.
3. Commit & push: the GitHub Action regenerates the docs, re-runs `merge_documentation.py`, and rebuilds the DOCX. To preview locally, run `python merge_documentation.py <subgroup>` followed by the `pandoc … --reference-doc=<subgroup>/input/template.docx` command.

> **Caveat (case-insensitive filesystems):** `gen-doc` writes one file per element, named exactly after it. A class `Protocol` and a slot `protocol` would produce `Protocol.md` / `protocol.md`, which collide on macOS/Windows. Avoid element names that differ only by case (e.g. name the slot `protocol_ref`).

## Subgroup Folder Structure

Each subgroup has its own folder. The folder structure is as follows:

```
subgroup-name/
├── input/
│   ├── schema.yaml
│   ├── data_x.yaml
│   ├── data_y.yaml
│   ├── ...
│   ├── data_z.yaml
│   ├── 1_topic_1.md
│   ├── 2_topic_2.md
│   ├── ...
│   ├── n_topic_n.md
│   ├── template.docx
├── misc/
│   ├── YYMMDD.md (for meeting notes)
│   ├── ...
├── output/
│   ├── docs/
│   ├── data_x.json
│   ├── ...
│   ├── data_x.ttl
│   ├── ...
│   ├── schema.json
│   ├── schema.ttl
│   ├── documentation_merged.md
│   ├── subgroup-name.docx
```

## Tutorial

There is also the folder `tutorial` that demonstrates, how to use the [LinkML](https://linkml.io/linkml/index.html) toolchain to create a data model for the eCH Specialist Group Political Affairs.