# lib/data

Example AKN XML documents carrying real ELI identifiers and AKN metadata/structure/content — the runtime
example-data library (distinct from `spec/input/examples/`, which is the standard's own authored examples).

Not yet populated.
