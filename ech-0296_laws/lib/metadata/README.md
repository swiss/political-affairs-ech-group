# lib/metadata

ELI properties: runtime definitions/handling for the metadata fields the eCH-0296 profile requires
(identification, publication, legal value, in-force status, etc. — see `pipeline/schema`'s crosswalk for
where these map to AKN). Generated/validated from `pipeline/schema`'s output, not hand-maintained here.

Not yet populated.
